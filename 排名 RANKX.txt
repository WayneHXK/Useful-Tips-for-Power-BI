排名 = 
RANKX(
    ALL(维度表[需要排名的列]),  // 清除对维度表的筛选
    [你的销售额度量值],           // 排名的依据表达式
    ,                            // 此参数可省略，或用于指定具体的排名值（这里用度量值本身）
    DESC,                        // DESC 表示降序排列
    Dense                        // 指定排名方式：Dense（并列排名占用同一名次，后续名次连续）或 Skip（并列排名占用同一名次，但后续名次不连续）
)